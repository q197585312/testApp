<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="heightLeft"
            type="float" />
        <variable
            name="itemClick"
            type="com.unkonw.testapp.libs.base.OnItemClickListener" />

        <variable
            name="itemBean"
            type="com.nanyang.app.data.Left" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="90dp"
        app:layoutHeight="@{heightLeft}"
        android:paddingBottom="10dp"
        android:onClick="@{(v)->itemClick.onItemClick(v,itemBean)}">

        <com.unkonw.testapp.libs.view.CircleImageView
            android:id="@+id/iv_content"
            imgUrl="@{itemBean.img}"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            app:borderWidth="@{itemBean.isSelected?2:0}"
            app:ease_border_color="@color/white"
            app:ease_radius="8dp"
            app:es_shape_type="rectangle"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/textView5"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{itemBean.text}"
            android:layout_margin="10dp"
            android:textAllCaps="true"
            android:textColor="@color/white"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>