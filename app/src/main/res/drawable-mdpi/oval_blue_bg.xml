<?xml version="1.0" encoding="utf-8"?><!-- 矩形 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/blue_height" />
    <!-- 描边采用那样的方式将外形轮廓线画出来，width表示笔的粗细，dashWidth表示小横线的宽度，dashGap表示小横线之间的距离 -->
    <!-- corners表示是有半径为3像素的圆角 -->
    <stroke
        android:width="1.5px"
        android:color="@color/white"></stroke>
</shape>